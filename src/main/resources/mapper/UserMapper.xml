<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.learn.springboot.blog.dao.UserDao">
    <sql id="BASE_TABLE">
        t_user
    </sql>

    <sql id="BASE_COLUMN">
        id,create_time,password,name,e_mail,
    </sql>

    <insert id="save" parameterType="com.learn.springboot.blog.entity.User">
        INSERT INTO
            <include refid="BASE_TABLE"/>
            <trim prefix="(" suffix=")" suffixOverrides=",">
                id,
                create_time,
                password,
                name,
                e_mail,
            </trim>
            <trim prefix="VALUES(" suffix=")" suffixOverrides=",">
                REPLACE(UUID(), '-', ''),
                UNIX_TIMESTAMP(NOW()),
                #{password, jdbcType=VARCHAR},
                #{userName, jdbcType=VARCHAR},
                #{eMail, jdbcType=VARCHAR},
            </trim>
    </insert>

    <select id="findAll">
        SELECT
            <include refid="BASE_COLUMN"/>
        FROM
            <include refid="BASE_TABLE"/>
    </select>
    <!-- 根据userName和密码查询用户 参数要加@Param注解，参数名和大括号中直一致 -->
    <select id="findByUserNameAndPassword">
        SELECT
            <include refid="BASE_COLUMN"/>
        FROM
            <include refid="BASE_TABLE"/>
        <where>
            <if test="userName != null">
                AND name = #{userName, jdbcType=VARCHAR}
            </if>
            <if test="password != null">
                AND password = #{password, jdbcType=VARCHAR}
            </if>
        </where>
    </select>
</mapper>